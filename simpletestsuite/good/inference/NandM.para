public class NandM { // running out of good names

   private lock Foo;

   ?policyof(p) int m(int p) { // M
      return p;
   }
   
   ?policyof(x) int n(int x) { // and N!
      return m(x);
   }
   
}
